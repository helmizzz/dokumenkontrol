RewriteEngine On

# Jika file atau folder fisik tidak ditemukan
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tambahkan .php secara otomatis di belakang layar
RewriteRule ^([^\.]+)$ $1.php [NC,L]
